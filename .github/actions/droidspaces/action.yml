name: Setup Droidspaces-OSS
description: Apply Droidspaces kABI fixes and configs from local patches
inputs:
  kernel_version:
    description: 'Kernel version (e.g., 6.6)'
    required: true

runs:
  using: composite
  steps:
    - name: Apply the SYSVIPC kABI fix (local patches)
      shell: bash
      working-directory: ${{ github.workspace }}/kernel/common
      run: |
        set -euo pipefail
        
        if [ "${{ inputs.kernel_version }}" = "6.12" ]; then
          PATCH_FILE="${{ github.workspace }}/kernel_patches/droidspaces/fix_sysvipc_kabi_a16-6.12.patch"
        else
          PATCH_FILE="${{ github.workspace }}/kernel_patches/droidspaces/fix_sysvipc_kabi_6_7_8.patch"
        fi
        
        echo "Applying kABI fix from: $PATCH_FILE"
        if patch -p1 --dry-run < "$PATCH_FILE" 2>/dev/null; then
          patch -p1 < "$PATCH_FILE"
          echo "Applied SYSVIPC kABI fix from local patch"
        else
          echo "⚠ SYSVIPC kABI fix doesn't apply on this kernel version, skipping" >&2
        fi

    - name: Export IPC symbols for module linkage
      shell: bash
      working-directory: ${{ github.workspace }}/kernel
      run: |
        echo >> "${{ github.workspace }}/kernel/common/ipc/namespace.c"
        echo 'EXPORT_SYMBOL_GPL(put_ipc_ns);' >> "${{ github.workspace }}/kernel/common/ipc/namespace.c"
        echo >> "${{ github.workspace }}/kernel/common/ipc/msgutil.c"
        echo 'EXPORT_SYMBOL_GPL(init_ipc_ns);' >> "${{ github.workspace }}/kernel/common/ipc/msgutil.c"

    - name: Enable Configs
      shell: bash
      working-directory: ${{ github.workspace }}/kernel
      run: |
        FRAGMENT="${{ github.workspace }}/wild_gki.fragment"
        cat >> "$FRAGMENT" << 'EOF'
        # Kernel configurations for full DroidSpaces support for GKI
        # Copyright (C) 2026 ravindu644 <droidcasts@protonmail.com>

        # IPC
        CONFIG_SYSVIPC=y
        CONFIG_POSIX_MQUEUE=y

        # HW Access Support
        CONFIG_DEVTMPFS=y
        EOF

        # IPC_NS, PID_NS: skip on 5.10 — Android GKI 5.10 has header stubs
        # that conflict with real implementations when these namespaces are
        # enabled (same pattern as CONFIG_USER_NS).
        if [ "${{ inputs.kernel_version }}" != "5.10" ]; then
          echo 'CONFIG_IPC_NS=y' >> "$FRAGMENT"
          echo 'CONFIG_PID_NS=y' >> "$FRAGMENT"
        else
          echo "  ⚠ skipping CONFIG_IPC_NS and CONFIG_PID_NS (not compatible on kernel 5.10)" >&2
          echo '# CONFIG_IPC_NS is not set' >> "$FRAGMENT"
          echo '# CONFIG_PID_NS is not set' >> "$FRAGMENT"
        fi
